{
  "name": "lumen-monorepo",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "turbo": "^2.4.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "framer-motion": "^12.34.0"
  }
}